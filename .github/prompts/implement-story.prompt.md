---
description: Implement a demo story using the right stack and acceptance criteria
argument-hint: STORY-001-task-api.md
---

Read the story file `demo/stories/${input:storyFile}`.

1. List acceptance criteria as a checklist.
2. Pick the correct agent workflow (backend → spring-boot-story, frontend → angular-story, both → full-stack-developer).
3. Implement only what the story requires under `backend/` and/or `frontend/`.
4. Run tests and provide curl or browser verification steps.
5. Do not commit unless I explicitly ask.

Story file: ${input:storyFile}
